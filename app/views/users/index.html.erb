<% @user.each do |user| %>
  <p><span><%= link_to user.email, user_path(user) %></span> 
    <span><%= user.name %></span> 
     <span><%= link_to "Edit User", user_path(user), method: :put, data: {confirm: "Are you sure you want to update this user?"} %>
   </span></p>
 <% end %>
