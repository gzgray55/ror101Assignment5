<% @user.each do |user| %>
  <p><span><%= link_to user.email, user_path(user) %></span> <span><%= user.name %></span> <span><%= link_to "Delete User", user_path(user), method: :delete, data: {confirm: "Are you sure you want to delete this user?"} %></span></p>
<% end %>
